= Changelog
:description: Ship timeline for the Linx Photos documentation hub (repository face).

Reader-facing Antora timeline: link:https://docs.linx.photos/photo-service/changelog/[Changelog on the docs site].

== 2026-09-14 -- page-context 0.6.6 author spacing

* Pin `@antora-supplemental/page-context` to `v0.6.6` so the lead/footer author name, avatar, and `via` stay spaced.

== Unreleased

* Pin `page-context` to `v0.6.6` (author spacing) and `page-edit` to `v0.2.3` (never prepend View|Edit before `<!DOCTYPE>`). Fixes View|Edit above the top navbar on the hub start page.
* Rebuild after Valentus rolling `v2` picks up the restored home icon chip + Version kicker label (when that UI bundle is released).

== 2026-09-10 — Refresh Facto extension pins

* Pin `page-context` to `v0.6.3` and `page-edit` to `v0.2.1` (tagged releases).
* Align Antora meta package to `^3.2.0`. Keep `asciidoctor-kroki` on `^0.18.1` (1.0.x needs Asciidoctor 4; Antora 3 still ships 2.x).
* Register `page-context/antora` keyword listing pages under `/photo-service/keywords/`.

== 2026-09-10 — Rename hub repo to docs

* GitHub repository is `LinxPhotos/docs` (was `docs.linx.photos`).
* Public site URL stays https://docs.linx.photos/ via Cloudflare DNS.

== 2026-09-09 — Valentus v2 + Facto

* Migrate UI bundle from Valentus `v1` to rolling `v2`.
* Wire Facto essentials: Lunr, Kroki, page-context, page-edit, build-stack.
* Thin supplemental UI to brand tokens + footer; drop forked header and vendored math (now in Valentus).
* Enable folder URLs (`indexify`) and page metadata on hub articles.

== 2026-08-12 — KaTeX stem math

* Enable `stem: latexmath` and KaTeX plumbing on every docs page.
